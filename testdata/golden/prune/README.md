# Prune goldens

Each case uses `meta.yaml`, `src.list`, optional `tgt.list`, and expected
stdout/stderr/exit files. Lists use the source and target properties required
by `internal/prune`; rows are newest-first.
