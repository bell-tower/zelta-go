// Package report is process-output rendering: match columns (cols.tsv) and
// backup JSON schema (json.tsv reference + BackupResult). Not part of the
// public Action Library — consumers build their own presentation from typed
// Results.
package report
