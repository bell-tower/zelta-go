# Policy (later)

YAML-like `zelta.conf` is hacky in Awk; Go may standardize while accepting behavioral parity on common layouts.

See `~/Code/zelta/zelta.conf` and `doc/wiki/conf/` for hierarchy. Stub package only for now.
