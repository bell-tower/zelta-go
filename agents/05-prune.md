# Prune / zprune (later)

- `zelta prune` — read-only candidate selection (logic today partly in match.awk)  
- `zprune` — destructive destroy wrapper + guard (`--match-endpoint` / `--no-prune-guard`)

Pixel-perfect efficiency for release versions is a project goal. Do not implement until match core is trustworthy.
